import type { HeadingItem } from "@/lib/markdown";
export default function ReaderTableOfContents({ headings, onSelect, onClose }: { headings: HeadingItem[]; onSelect:(id:string)=>void; onClose:()=>void }) { return <div className="toc-panel" role="dialog" aria-label="목차"><div className="panel-title"><strong>목차</strong><button onClick={onClose} aria-label="닫기">×</button></div><nav>{headings.length ? headings.filter(h => h.level <= 3).map(h => <button key={h.id} className={`toc-level-${h.level}`} onClick={() => onSelect(h.id)}>{h.text}</button>) : <p>표시할 목차가 없습니다.</p>}</nav></div> }
