import type { Dispatch, SetStateAction } from "react";
import type { ReaderSettings as Settings } from "@/types/document";
export default function ReaderSettings({ value, onChange, onClose, embedded = false }: { value: Settings; onChange: Dispatch<SetStateAction<Settings>>; onClose: () => void; embedded?: boolean }) { const set = <K extends keyof Settings>(key: K, next: Settings[K]) => onChange((old) => ({ ...old, [key]: next })); return <div className={`settings-panel ${embedded ? "is-embedded" : ""}`} role="dialog" aria-label="읽기 설정"><div className="panel-title"><strong>읽기 설정</strong>{!embedded&&<button onClick={onClose} aria-label="닫기">×</button>}</div><fieldset><legend>읽기 방식</legend><div className="segmented"><button className={value.mode === "scroll" ? "active" : ""} onClick={() => set("mode", "scroll")}>세로 스크롤</button><button className={value.mode === "page" ? "active" : ""} onClick={() => set("mode", "page")}>페이지 넘김</button></div></fieldset><Control label="글자 크기" value={`${value.fontSize}px`} minus={() => set("fontSize", Math.max(16, value.fontSize - 1))} plus={() => set("fontSize", Math.min(24, value.fontSize + 1))}/><Control label="행간" value={value.lineHeight.toFixed(1)} minus={() => set("lineHeight", Math.max(1.6, +(value.lineHeight - .1).toFixed(1)))} plus={() => set("lineHeight", Math.min(2.2, +(value.lineHeight + .1).toFixed(1)))}/><fieldset><legend>본문 폭</legend><div className="segmented">{(["narrow","default","wide"] as const).map((x, i) => <button key={x} className={value.width === x ? "active" : ""} onClick={() => set("width", x)}>{["좁게","기본","넓게"][i]}</button>)}</div></fieldset><fieldset><legend>글꼴</legend><div className="segmented"><button className={value.fontFamily === "sans" ? "active" : ""} onClick={() => set("fontFamily", "sans")}>고딕</button><button className={value.fontFamily === "serif" ? "active" : ""} onClick={() => set("fontFamily", "serif")}>명조</button></div></fieldset></div> }
function Control({ label, value, minus, plus }: { label:string; value:string; minus:()=>void; plus:()=>void }) { return <fieldset><legend>{label}</legend><div className="stepper"><button onClick={minus} aria-label={`${label} 줄이기`}>−</button><output>{value}</output><button onClick={plus} aria-label={`${label} 늘리기`}>＋</button></div></fieldset> }
